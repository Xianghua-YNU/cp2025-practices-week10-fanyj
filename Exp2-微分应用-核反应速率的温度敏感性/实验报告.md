# 3-α 反应速率的温度敏感性指数计算实验报告
## 一、实验目的
1. 理解3-α反应速率对温度的依赖性
2. 掌握数值微分方法（有限差分）计算温度敏感性指数的原理和实现
3. 分析不同温度下反应速率的敏感性特征及其物理意义
## 二、实验原理
### 2.1 3-α反应速率
3-α反应是一个重要的核聚变过程，将三个氦-4原子核聚变成一个碳-12原子核：

$${}^4\mathrm{He} + {}^4\mathrm{He} + {}^4\mathrm{He} \rightarrow {}^{12}\mathrm{C}$$

其能量产生率表达式为：

$$q_{3\alpha} = 5.09\times 10^{11} \rho^2 Y^3 T_8^{-3} e^{-44.027/T_8} \mathrm{erg g^{-1}~s^{-1}}$$

### 2.2 温度敏感性指数
温度敏感性指数 $\nu$ 定义为反应速率对数对温度对数的导数：

$$\nu = \left . \frac{d\log q}{d\log T}\right | _{T_0} = \left ( \frac{T}{q} \frac{dq}{dT} \right ) _{T_0}$$

使用前向差分法近似计算导数：

$$\nu(T_0) \approx \frac{T_0}{q(T_0)} \frac{q(T_0 + h \cdot T_0) - q(T_0)}{h \cdot T_0}$$

## 三、代码实现
（此处粘贴您实现的代码，包括反应速率函数和温度敏感性指数计算函数）

```python
import numpy as np
import matplotlib.pyplot as plt


def q3a(T):
    """
    计算 3-alpha 反应速率中与温度相关的部分 q / (rho^2 Y^3)
    输入: T - 温度 (K)
    返回: 速率因子 (erg * cm^6 / (g^3 * s))
    """
    T8 = T / 1.0e8  # 以 10^8 K 为单位的温度
    # 避免 T8 过小导致除零或溢出错误 (虽然在此问题中 T 的范围较大，一般不会遇到)
    if T8 <= 0:
        return 0.0
    rate_factor = 5.09e11 * T8**(-3.0) * np.exp(-44.027 / T8)
    return rate_factor


def plot_rate(filename="rate_vs_temp.png"):
    """绘制速率因子随温度变化的 log-log 图"""
    T_values = np.logspace(np.log10(3.0e8), np.log10(5.0e9), 100)  # 温度范围 3e8 K to 5e9 K
    q_values = [q3a(T) for T in T_values]

    fig, ax = plt.subplots()
    ax.loglog(T_values, q_values)
    ax.set_xlabel("Temperature T (K)")
    ax.set_ylabel(r"$q_{3\alpha}/(\rho^2 Y^3)$  (erg cm$^6$ g$^{-3}$ s$^{-1}$)")
    ax.set_title("3-α Reaction Rate Factor vs Temperature")
    ax.grid(True, which="both", ls=":")  # show both major and minor grid lines
    plt.savefig(filename)
    plt.show()


if __name__ == "__main__":
    # 计算并打印 nu 值
    print("   温度 T (K)    :   ν (敏感性指数)")
    print("--------------------------------------")

    temperatures_K = [1.0e8, 2.5e8, 5.0e8, 1.0e9, 2.5e9, 5.0e9]
    h = 1.0e-8  # 扰动因子

    for T0 in temperatures_K:
        q_T0 = q3a(T0)
        if q_T0 == 0:  # 避免除以零
            nu = np.nan  # Not a Number
        else:
            delta_T = h * T0
            q_T0_plus_deltaT = q3a(T0 + delta_T)

            # 使用前向差分计算 dq/dT
            dq_dT_approx = (q_T0_plus_deltaT - q_T0) / delta_T

            # 计算 nu
            nu = (T0 / q_T0) * dq_dT_approx

        # 格式化输出
        print(f"  {T0:10.3e} K : {nu:8.3f}")

    plot_rate()
```



## 四、实验结果与分析
### 4.1 温度敏感性指数计算结果

控制台
   温度 T (K)    :   ν (敏感性指数)
--------------------------------------
   1.000e+08 K :   41.027
   2.500e+08 K :   14.611
   5.000e+08 K :    5.805
   1.000e+09 K :    1.403
   2.500e+09 K :   -1.239
   5.000e+09 K :   -2.119
### 4.2 反应速率随温度变化的可视化分析
（在此处插入您绘制的反应速率随温度变化的对数-对数图，并进行分析说明）
![download](https://github.com/user-attachments/assets/683d74b3-74a2-49ec-b183-4e011a9d2a3f)
从图中可以看出，在对数 - 对数坐标下，反应速率与温度呈现出非线性关系。随着温度升高，反应速率显著增大。这符合 3 - α 反应速率公式中温度的指数和指数函数关系，表明温度对反应速率有很强的影响。
### 4.3 结果分析
1. 温度敏感性随温度变化的趋势分析：
   
   - 在低温区域（~10⁸ K）的敏感性特征
   -  在低温区域，敏感性指数 ν 值较大，这意味着反应速率对温度的变化非常敏感。
   - 在高温区域（~10⁹ K）的敏感性特征
   - 在高温区域，敏感性指数 ν 值相对低温区域变小，说明反应速率对温度变化的敏感程度降低。
   - 敏感性指数的变化趋势及其物理意义
   - 敏感性指数随温度升高而减小，反映了 3 - α 反应速率在低温时对温度依赖程度高，而高温时依赖程度相对降低。这对于理解恒星内部能量产生机制很重要，低温区域的高敏感性意味着温度的调节对反应速率和能量产生影响大，是恒星早期演化中能量平衡的关键因素。
2. 数值计算方法的精度分析：

   - 步长选择（h = 10⁻⁸）的合理性
   - 步长 h = 10⁻⁸ 是一个相对较小的值，从理论上可以在一定程度上减小截断误差，使前向差分更接近真实导数。
   - 可能的数值误差来源及其影响
   - 数值误差主要来源包括截断误差和舍入误差。截断误差是由于用有限差分近似导数导致的，步长越大，截断误差越大
## 五、讨论与思考
1. 为什么在低温（T₀ = 10⁸ K）时 $\nu$值特别大？这对恒星演化有什么意义？
   在低温时，3 - α 反应速率公式中的指数项\(e^{−44.027/T_8}\) 对温度变化极为敏感，温度的微小改变会使指数项大幅变化，进而导致反应速率显著改变，所以 ν 值大。在恒星演化早期，核心温度较低，此时 3 - α 反应对温度的高敏感性使得恒星能够通过调节温度来控制反应速率和能量产生，对恒星从原恒星向主序星的演化过程中的能量平衡和结构稳定起到关键作用 。
3. 数值微分中步长的选择（h = 10⁻⁸）是如何平衡截断误差和舍入误差的？
   截断误差与步长的高阶项相关，步长越小，截断误差越小；舍入误差则随着步长减小，计算次数增多而增大。  
5. 如果使用中心差分法替代前向差分法，计算结果会有什么不同？精度会如何改变？
   中心差分法公式为\(\nu(T_0) \approx \frac{T_0}{2q(T_0)} \frac{q(T_0 + h\cdot T_0) - q(T_0 - h\cdot T_0)}{h\cdot T_0}\) ，它的截断误差是二阶的，比前向差分法（一阶截断误差）理论上精度更高。使用中心差分法计算的敏感性指数可能会更接近真实值，误差更小。
7. 反应速率对温度的强依赖性对恒星内部的温度调节机制有什么影响？
   反应速率对温度的强依赖性使得恒星内部存在一种负反馈调节机制。当温度升高时，反应速率会迅速增大，产生更多能量，推动恒星膨胀，进而使温度降低；当温度降低时，反应速率大幅减小，能量产生减少，恒星收缩，温度又会回升。这种机制有助于维持恒星内部的温度和能量平衡，保证恒星在较长时间内稳定地进行核聚变反应。
## 六、结论
（总结实验的主要发现和结论）
通过本次实验，成功实现了 3 - α 反应速率函数和温度敏感性指数的数值计算。明确了 3 - α 反应速率对温度的依赖性特征，低温时反应速率对温度高度敏感，高温时敏感性降低。利用前向差分法计算温度敏感性指数，并对数值计算方法的精度进行了分析，了解了步长选择对截断误差和舍入误差的影响。
