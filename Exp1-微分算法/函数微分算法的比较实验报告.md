# 函数微分算法的比较实验报告
## 一、实验目的
1. 掌握中心差分法和Richardson外推法计算函数导数的原理和实现方法
2. 通过数值实验比较两种数值微分方法的精度特性
3. 理解不同数值微分方法的适用场景和局限性
## 二、实验原理
### 2.1 待分析函数
$$f(x) = 1 + \frac{1}{2}\tanh(2x)$$

### 2.2 中心差分法
- 计算公式：
  $$f'(x)\approx \frac{f(x+h)-f(x-h)}{2h}$$
### 2.3 Richardson外推法
- 计算公式：
  $$D_{i,j} = \frac{4^j D_{i,j-1} - D_{i-1,j-1}}{4^j - 1},\quad D_{i,0}=\frac{f(x+h_i)-f(x-h_i)}{2h_i}$$
## 三、代码实现
（此处简要说明代码实现的主要步骤和或插入代码）
```python
def get_analytical_derivative():
    x = symbols('x')
    expr = diff(1 + 0.5 * tanh(2 * x), x)
    return lambdify(x, expr)
def calculate_central_difference(x, f):
    dy = []
    for i in range(1, len(x)-1):
        h = x[i+1] - x[i]
        dy.append((f(x[i+1]) - f(x[i-1])) / (2 * h))
    return np.array(dy)
def richardson_derivative_all_orders(x, f, h, max_order=3):
    R = np.zeros((max_order + 1, max_order + 1))
    for i in range(max_order + 1):
        hi = h / (2**i)
        R[i, 0] = (f(x + hi) - f(x - hi)) / (2 * hi)
    for j in range(1, max_order + 1):
        for i in range(max_order - j + 1):
            R[i, j] = (4**j * R[i+1, j-1] - R[i, j-1]) / (4**j - 1)
    return [R[0, j] for j in range(1, max_order + 1)]
```

## 四、实验结果与分析
### 4.1 导数计算结果对比![download](https://github.com/user-attachments/assets/f29c2693-8138-4505-beac-7eb0d854548c)

（插入三种方法计算结果的对比图）

### 4.2 误差分析 
#### 4.2.1 中心差分法误差分析
（插入中心差分法误差随步长变化的log-log图及分析）
#### 4.2.2 Richardson外推法误差分析
（插入不同阶数Richardson外推法误差随步长变化的log-log图及分析）

## 五、实验讨论
### 5.1 两种方法的优缺点分析
1. 中心差分法
   - 优点：原理简单直观：基于导数的基本定义，公式易于理解和实现，对于初学者来说容易上手。计算复杂度低：计算过程相对简单，仅涉及函数在少数几个点的取值计算
   - 缺点：精度有限：误差阶数为 2，在一些对精度要求较高的应用中，可能无法满足需求。
步长敏感性高：步长的选择对计算结果影响较大，步长过大误差较大，步长过小又会受到舍入误差的影响
2. Richardson外推法
   - 优点：高精度：通过对不同步长的结果进行外推，可以显著提高导数计算的精度，可调节精度：通过调整外推阶数，可以根据实际需求灵活控制计算精度，在一定程度上平衡精度和计算效率。
   - 缺点：计算复杂度高：需要计算多个不同步长下的中心差分值，并进行多次迭代计算，计算量随着外推阶数的增加呈指数增长，对计算资源要求较高。
稳定性问题：在实际计算中，过高的外推阶数可能会引入更多的误差，导致计算结果不稳定，需要谨慎选择外推阶数 。
